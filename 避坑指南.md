# MCCombatStatus-JNI 避坑指南

> 做 Minecraft JNI 注入工具（以及任何 JNI/注入开发）时踩过的所有坑。
> 每一条都是本项目真机排查中实际遇到并确认的。

---

## 一、JNI 核心坑（最容易崩溃/翻车）

### 1. `GetMethodID` 的第一个参数 = 方法**所属的类**，不是对象的类！

```cpp
// ❌ 错误: GetObjectClass 返回的是"对象的类", 用它找 getName 会找到
//    ClassLoader.getName() (因为 AppClassLoader 类上有这个方法),
//    然后调用在 Class 实例上 -> 类型不匹配 -> JVM 段错误!!
jclass lc = env->GetObjectClass(loader);
jmethodID nmM = env->GetMethodID(lc, "getName", "()Ljava/lang/String;");
env->CallObjectMethod(lc, nmM);   // 💥 崩溃

// ✅ 正确: 方法属于 java/lang/Class
jclass clsCls = env->FindClass("java/lang/Class");
jmethodID nmM = env->GetMethodID(clsCls, "getName", "()Ljava/lang/String;");
env->CallObjectMethod(lc, nmM);   // lc 是 Class 实例, 调用 Class.getName() ✓
```

> 这是本项目第一次真机崩溃的根因。**GetObjectClass(obj) 只告诉你 obj 是什么类，
> 不能拿它的返回值去 GetMethodID。**

### 2. 字段/方法签名必须与声明**完全一致**（包括具体类型！）

```cpp
// ❌ 错误: Launch.classLoader 的声明类型是 LaunchClassLoader, 不是 ClassLoader
env->GetStaticFieldID(launch, "classLoader", "Ljava/lang/ClassLoader;");  // 返回 NULL

// ✅ 正确
env->GetStaticFieldID(launch, "classLoader",
                      "Lnet/minecraft/launchwrapper/LaunchClassLoader;");
```

字段签名不匹配时 `GetFieldID` 静默返回 NULL（挂异常）——**不报错但拿不到**，
排查时极易忽略。签名里的类名必须是**声明时的精确类型**。

### 3. pending exception 污染（静默失败元凶）

JNI 函数在**已有 pending exception** 时调用，行为是**未定义**的
（HotSpot 通常直接返回 NULL/0 不执行）。排查特征：
"明明代码没问题，但某个 JNI 调用一直返回 NULL 且 ExceptionCheck 为假"。

```cpp
// ❌ 异常记录代码本身又挂异常, 且没清理就 return
jthrowable ex = env->ExceptionOccurred();
env->ExceptionClear();
jclass exCls = env->GetObjectClass(ex);
jmethodID getName = env->GetMethodID(exCls, "getName", ...);  // 又错了(坑1)!
// ... 没清理就 return -> 后续所有 JNI 调用被污染 -> 静默 NULL

// ✅ 规则: 任何 Get*/Call* 之后, 要么清理异常, 要么保证异常路径完整
if (env->ExceptionCheck()) env->ExceptionClear();
```

**规则**：每个可能失败的 JNI 调用后，失败路径必须 `ExceptionClear()`；
异常记录代码本身也要遵守坑 1/坑 2。

### 4. 双份类副本（同名不同类）

同一个类名，被**不同类加载器**各加载一份 = 两个完全不同的类：

- `FindClass`（JNI 默认）用线程上下文类加载器 → 可能加载"副本"
- 游戏用 LaunchClassLoader 加载"真类"（静态字段已初始化）

后果：真机 `getMinecraft()` 返回 null（副本的静态字段从未被 main 初始化）、
字段 ID 与游戏实例不通用（会抛异常）。

**对策**：找到游戏的类加载器（`Launch.classLoader` / 线程 context loader），
用 `Class.forName(name, false, gameLoader)` 加载——同一个 loader 加载的
类才是游戏用的那份。`initialize=false` 很重要：类通常已被游戏初始化过，
重复初始化可能抛异常。

**终极判定法（V54 实测）**："解析成功"不等于"拿到游戏实例"——
副本类结构完全一致（成员名全对），但静态字段从未被 main 初始化，
`getMinecraft()` 返回 null。**解析成功后立即调用 getter 验证返回值**：
```cpp
// 解析成功后:
jobject inst = env->CallStaticObjectMethod(res.mcClass, res.getMinecraft);
if (inst == NULL) { /* 副本! 判无效, 继续试其他 map/loader */ }
```
这一条同时解决"假成功死循环"（副本抢占正确映射的位置）。

### 5. 局部引用泄漏（长循环必炸）

JVM 每个线程的局部引用表容量有限（最小保证 16）。长循环里每次
`CallXXXMethod`/`FindClass` 都产生局部引用，不释放会溢出崩溃。

```cpp
while (true) {
    env->PushLocalFrame(32);   // 开"篮子"
    jobject x = env->CallObjectMethod(...);
    // ... 随便创建引用 ...
    env->PopLocalFrame(NULL);  // 一次释放全部
}
```

`GetStringUTFChars` 必须配对 `ReleaseStringUTFChars`。

### 6. 枚举比较用 `IsSameObject`，不要比指针

```cpp
jobject typeObj = env->GetObjectField(mop, typeOfHitField);
jobject entConst = env->GetStaticObjectField(typeClass, entityConstField);
bool isEntity = env->IsSameObject(typeObj, entConst);  // ✅
```

### 7. 线程必须 attach / detach

`CreateThread` 的原生线程不认识 JVM，必须
`vm->AttachCurrentThread((void**)&env, NULL)` 后才能调 JNI，
结束前 `vm->DetachCurrentThread()`。JNIEnv 不能跨线程使用。

---

## 二、注入层坑

### 8. `LoadLibrary` 模块缓存（改代码不生效！）

同路径的 DLL 在进程里**只加载一次**。改完代码复制覆盖同名 DLL 再注入，
**加载的还是旧代码**！

```bat
:: 排查: 看进程实际加载了哪些 DLL
powershell "(Get-Process -Id <pid>).Modules | Where-Object {$_.FileName -like '*MCCanAttack*'}"
```

**对策**：调试期每次用**新文件名**（`MCCombatStatusJni_v28.dll`）注入；
injector 加 `-dll <路径>` 参数指定。

### 9. 防重复注入 guard 与"接管"逻辑

- 简单 guard（共享内存存在就拒绝）→ 卡死的 worker 永远无法替换
- 改进：`ready==0 && tick==0`（疑似卡死）时允许新 worker 接管
- 但多个 worker 同时写共享内存 → 字段竞争、状态混乱
- **调试期最干净的办法：重启游戏进程**（老 worker 无法远程杀死）

### 10. 共享内存布局必须严格一致

DLL 与读端（injector）的结构体必须**逐字段一致**（含顺序、大小）。
版本迭代中加字段时，旧版 worker 按旧布局写入会**错位覆盖**新字段
（表现为"字段值奇怪/读不到"）。`#pragma pack(push,1)` 固定对齐。

### 11. 多 worker 竞争的症状识别

- tick 乱跳 / 字段值来回变
- 同一字段不同时刻显示不同版本的格式
- 注入新版本后行为没变化（被 guard 拦 / 被旧 worker 覆盖）
- **症状 → 排查：`Get-Process Modules` 看加载了哪些 DLL**

---

## 三、运行时结构认知坑（Minecraft 特有）

### 12. 磁盘 ≠ 运行时（Forge 的反混淆在内存里）

`javap` 反编译 jar 看到的（`ave`/`A()`）**不等于运行时**！

- **Forge 1.8.9**：FML 的 `DeobfuscationTransformer` 类加载时转换：
  类名 `ave` → `net.minecraft.client.Minecraft`（MCP 名），成员 `A` → `func_71410_x`（SRG 名）
- **Forge 1.20.1**：ModLauncher 转换：类名 → Mojang 官方名，成员 → **MCP stable 名**（`m_91087_` 格式）

**必须用运行时工具验证**：
```bat
jmap -clstats <pid>                    :: 每个类加载器加载了多少类
jcmd <pid> GC.class_histogram          :: 已加载的类名
jcmd <pid> GC.class_histogram ^| grep -i minecraft
```

### 12b. MCP stable 名（m_xxx）是 Forge 1.17+ 的运行时成员名

Forge 1.17+ 的 reobf 目标 = **MCP stable 名**（不是混淆名也不是官方名！）。
查 stable 名：本地 Forge 安装的 mcp_config 里有映射文件：
```
D:/minecraft/.minecraft/libraries/de/oceanlabs/mcp/mcp_config/<版本>/mcp_config-<版本>-mappings-merged.txt
```
格式 `混淆名 签名 stable名`（如 `N ()Lenn; m_91087_`）。

### 12c. 1.8-1.12 的 SRG 名查询：deobfuscation_data

Forge 1.8-1.12 的 FML jar 里有 `deobfuscation_data-<版本>.lzma`（运行时反混淆数据）：
```
unzip -o forge-1.12.2-<版本>.jar deobfuscation_data-1.12.2.lzma
python -c "import lzma; open('d.txt','wb').write(lzma.open('deobfuscation_data-1.12.2.lzma', format=lzma.FORMAT_ALONE).read())"
```
格式：`CL: 混淆类名 MCP类名`、`FD: 混淆类/字段 SRG字段`、`MD: 混淆类/方法 签名 SRG方法 签名`。
1.12.2 实测：`CL: bib net/minecraft/client/Minecraft`、`MD: bib/z ()Lbib; ...func_71410_x`。

### 13. `-Dfml.ignorePatchDiscrepancies=True` 的环境差异

PCL 等启动器给 Forge 1.8.9 加这个参数 + 未修补 jar 时，
FML 跳过安装期 patch、靠运行时反混淆——**这是"类名 MCP + 成员 SRG"形态的成因**。
标准安装器装的 Forge 可能不同（未实测，见技术文档"困惑 2"）。

### 14. 同一版本有多种合法形态

1.8.9 至少有 4 种运行时形态（原版混淆 / MCP / Forge-mcp类名+SRG成员 /
可能的标准 Forge 变体）——**工具必须全部支持并自动尝试**，不能只做一种。

---

## 四、调试方法论坑

### 15. 假客户端测试的局限

假客户端（普通 java 启动）只有单一类加载器，**永远测不出**：
- 双份类副本
- LaunchClassLoader / TransformingClassLoader 特性
- FML/ModLauncher 运行时反混淆（类名/成员名转换）
- pending exception 污染（假客户端调用链简单，不易触发）

**但它适合快速验证**：映射表逻辑、判定公式、JNI 调用正确性、崩溃回归。
**真机验证不可替代**——本项目所有核心 bug（除 JNI 崩溃）都是真机才暴露的。

### 15b. 线程遍历找游戏类加载器（1.17+ ModLauncher）

Forge 1.17+ 的游戏类在 `TransformingClassLoader`（不是 app loader！）。
获取方式：遍历 `Thread.getAllStackTraces()` 的线程 context classloader，
探测 `findLoadedClass(游戏类)` 命中者。**必须加超时**（本项目 10 秒）防卡死。

### 15c. ModLauncher 环境下禁止用 `Class.forName`

`TransformingClassLoader` 重写了 loadClass，`Class.forName(name, false, tcl)`
会抛 ClassNotFoundException（即使类已加载）。**必须用 `findLoadedClass`**
（protected 方法，JNI 可调用）——已加载的类直接拿。

### 16. 诊断要"写文件"，不要只写共享内存

多 worker 竞争时共享内存字段互相覆盖，读不到可靠诊断。
**对策**：探测结果直接追加写文件（`probe.log`），不受竞争影响。

### 17. 保留历史版本，二分定位回归

每次改动保留编译产物（`dll_V20.dll`、`dll_V21.dll`...），
崩溃/回归时逐个注入二分，几分钟定位引入版本。
本项目靠这个在 30 分钟内定位了 JVMTI 崩溃点。

### 18. 先确认进程环境再下结论

- `tasklist` 看有几个 java（残留进程会干扰测试脚本抓 PID）
- `Get-CimInstance Win32_Process -Filter 'ProcessId=...'` 看启动命令
- 测试脚本里 `head -1` 抓 PID 在多个进程时不可靠

### 19. 新增检测项时必须逐方法核对映射（双持系统教训）

"SRG 名跨 1.8-1.12 稳定"只对**已验证过的成员**成立。1.12.2 真机暴露：
**1.9 起 Minecraft 引入双持，无参 `getHeldItem()`（`func_70694_bm`）被移除**，
1.12.2 必须用 `getHeldItemMainhand`（`func_184614_ca`）。加映射时逐方法
用 `deobfuscation_data-<版本>.lzma` 核对是否存在（本项目用
`placeReady` 诊断字段快速区分"真空手"vs"链未解析成功"）。

### 20. 假客户端里瞄准目标类型要对

假客户端测试把 Entity 基类实例当瞄准目标时，`IsInstanceOf(livingClass)`
判定不过 → canAttack 恒 0 但 canPlace 正常（误以为映射错）。瞄准目标
必须是 EntityLivingBase 子类实例。

---

## 五、最终检查清单（发布前）

- [ ] 假客户端 7 套全过（map 正确 + canAttack/canPlace 各阶段 + 不崩溃）
- [ ] 真机至少一个环境实测（map/env/loader 显示正确）
- [ ] `Launch.classLoader` 字段签名用精确类型
- [ ] 所有 Get*/Call* 失败路径 ExceptionClear
- [ ] 长循环 PushLocalFrame/PopLocalFrame
- [ ] 共享内存结构体 DLL 与读端一致
- [ ] JVMTI 代码在无 agent 环境禁用（编译开关）
- [ ] 诊断日志写文件（probe.log）
- [ ] 放置物成员是"可选解析"——失败仅 canPlace=0，不拖垮 canAttack
