# MCCombatStatus-JNI 技术文档：总思路 / 架构原理 / 调试历程 / 避坑复盘

> 本文记录本项目的**设计总思路**、完整技术架构，以及"支持 Forge 1.8.9"过程中
> 真机排查的全过程复盘——包括最终结论和**仍未完全解开的困惑**。

---

## 〇、总思路（设计总纲）

> 从需求到方案的完整思考链：为什么这么做、每一步的理由。

### 0.1 需求拆解

**需求**：实时获取"玩家当前是否能攻击（准星瞄准了可攻击的生物）"。

**约束**：
1. 不改游戏文件、不重打包、不装 Mod（对原版/Forge/Fabric 通用）
2. 尽量多版本兼容（1.8.9 / 1.12.2 / 1.20.1 及各自 Forge）
3. 外部程序要能随时读取结果

### 0.2 方案选型（为什么是"注入 DLL + JNI"）

| 候选方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **注入 DLL + JNI** | 不改游戏；JNI 是官方接口最稳定；跨版本只需换映射表 | 需注入（杀软敏感）；要处理类加载器 | ✅ 采用 |
| 做 Mod（Forge/Fabric） | 开发简单 | 每个版本/加载器都要写一套；原版用不了 | ✗ |
| 外部读内存（找基址） | 无注入 | 基址随版本/加载器全变；Java 对象在堆里不好定位 | ✗ |
| 抓包/钩子 | — | 拿不到内部状态 | ✗ |

### 0.3 核心数据通路的设计

```
游戏内部（Java 堆）                    外部
Minecraft 单例 ──▶ JNI 读字段/调方法 ──▶ 判定 ──▶ 共享内存 ──▶ injector/任何程序
```

- **为什么读 `objectMouseOver`**：游戏每 tick 自己算好准星射线检测结果，
  我们只读不算，永远和游戏同步（第 2 章）。
- **为什么共享内存**：跨进程通信最轻；任何语言都能 `OpenFileMapping` 读；
  不依赖被注入进程配合。
- **为什么还要 probe.log 文件日志**：调试期多 worker 竞争时共享内存字段
  互相覆盖，文件日志不受影响（避坑 16）。

### 0.4 适配层设计："映射表 + 加载器候选链"双保险

Minecraft 不同环境的类名/成员名差异巨大（第 2 章 8 种形态），设计上分成两层：

```
第 1 层: 加载器候选链 —— 解决"类从哪来"
   Launch.classLoader → 线程遍历(context loader) → app loader
   （每轮双加载器同轮尝试，避免状态翻转不同步）

第 2 层: 10 套映射表 —— 解决"类叫什么"
   按顺序尝试, 哪套全部解析成功用哪套 (map= 字段显示)
```

**关键原则**：加载器决定"用哪份类"（真类 vs 副本），映射表决定"名字是什么"。
两层独立，互相兜底。

### 0.5 稳定性设计（踩坑后的沉淀）

| 设计 | 解决的问题 |
|---|---|
| 状态结构体版本化 + `#pragma pack` | 跨进程布局一致 |
| 防重复注入 guard + 卡死接管（ready/tick 健康检查） | 重复注入/死 worker |
| 每个 JNI 调用后检查并清理异常 | pending exception 污染 |
| 长循环 PushLocalFrame/PopLocalFrame | 局部引用表溢出 |
| findLoadedClass 优先、forName 兜底 | ModLauncher 重写 loadClass |
| 10 秒超时保护（线程遍历） | 游戏繁忙时防卡死 |
| 假客户端 + 真机双测试 | 假客户端验逻辑，真机验加载器现实 |
| 历史版本保留 + 二分回归 | 快速定位回归引入点 |

### 0.6 扩展新版本的工作流（查表式）

```
1. 启动目标版本，注入当前 DLL，看 probe.log 的探测结果
   （mccls:OK/X + 方法名格式 M:xxx）
2. 确定形态：
   - 类名: MCP? Mojang? 混淆?  （jcmd class_histogram / flc 探测）
   - 成员名: A? func_? m_? getInstance? （反射枚举 M: 行）
3. 查具体名字：
   - 1.8-1.12: deobfuscation_data-<版本>.lzma（python lzma 解压）
   - 1.17+: mcp_config mappings-merged.txt（tsrg2）
4. 往 kAllMaps[] 加一张表，编译，真机验证
```

---

## 一、总体架构

```
┌──────────────┐   CreateRemoteThread    ┌──────────────────────────────────┐
│  injector.exe │ ──── + LoadLibraryA ──▶ │  java.exe (Minecraft)            │
│              │                          │  ┌────────────────────────────┐ │
│ 1. 找窗口     │                          │  │ MCCanAttackJni.dll          │ │
│ 2. 注入 DLL   │                          │  │  DllMain                   │ │
│ 3. 读共享内存  │                          │  │   └─ JniWorker 线程          │ │
│ 4. 打印状态   │                          │  │      ├─ 等 jvm.dll           │ │
└──────┬───────┘                          │  │      ├─ JNI_GetCreatedJavaVMs│ │
       │                                  │  │      ├─ AttachCurrentThread  │ │
       │                                  │  │      ├─ 找游戏类加载器        │ │
       │                                  │  │      ├─ 10 套映射依次解析      │ │
       │                                  │  │      └─ 每 50ms 判定+写状态    │ │
       │                                  │  └────────────┬───────────────┘ │
       │                                  └───────────────┼─────────────────┘
       └────────── 读取 ◀─────────────────── 共享内存 ─────┘
             Local\MCCanAttackStatus_<pid>   (+ probe.log 诊断文件)
```

### 关键设计决策

| 决策 | 理由 |
|---|---|
| 10 套命名映射自动尝试 | 不同版本/客户端运行时类名不同，映射表化最灵活 |
| 游戏类加载器优先 | 避免双份类副本（见避坑指南） |
| 共享内存 + 文件日志双通道 | 共享内存实时、文件日志不受进程竞争影响 |
| 假客户端 + 真机双测试 | 假客户端验证逻辑正确性，真机验证类加载器现实 |

---

## 二、运行时命名体系全景（本项目实测结论）

同一个 Minecraft 版本，在不同环境/加载器下**类名和成员名完全不同**：

| 环境 | 类名示例 | 成员名示例 | 来源 |
|---|---|---|---|
| 官方 jar（磁盘） | `ave` | `A()` / `h` / `s` | javap 反编译 |
| MCP 反混淆客户端 | `net.minecraft.client.Minecraft` | `getMinecraft` / `thePlayer` | MCP 映射 |
| **Forge 1.8.9 运行时** | **`net.minecraft.client.Minecraft`** | **`func_71410_x` / `field_71439_g`** | **jcmd/jmap/probe.log 实测** |
| **Forge 1.20.1 运行时** | **`net.minecraft.client.Minecraft`** | **`m_91087_` / `f_91074_`** | **mcp_config stable 映射实测** |
| 1.20.1 原版/Fabric | `enn` | `N()` / `t` / `w` | Mojang 官方映射 |
| 1.20.1 Forge（理论形态） | `net.minecraft.client.Minecraft` | `getInstance` / `player` | Mojang 官方映射 |

**核心发现 1（Forge 1.8.9）**：FML 的 `DeobfuscationTransformer` 在**类加载时**做运行时反混淆：
- **类名** → 转成 **MCP 名**（`ave` → `net.minecraft.client.Minecraft`）
- **成员名** → 转成 **SRG 名**（`A` → `func_71410_x`）

**核心发现 2（Forge 1.20.1）**：ModLauncher 的 `TransformingClassLoader`：
- **类名** → 用 **Mojang 官方名**（`net.minecraft.client.Minecraft`）
- **成员名** → 用 **MCP stable 名**（`getInstance` → `m_91087_`，`player` → `f_91074_`）
- 游戏类加载器是 `cpw.mods.modlauncher.TransformingClassLoader`（可用线程遍历找到）
- `Class.forName` 在 TCL 上会抛 CNFE（ModLauncher 重写 loadClass）→ 必须用 `findLoadedClass`

### 证据链（真机实测）

1. **磁盘**：`javap` 反编译版本 jar → `ave` 类含 `A()/h/s`（原版混淆）
2. **加载者**：`jmap -clstats <pid>` → `LaunchClassLoader 3872 个类`（游戏类加载器）
3. **运行时类名**：`jcmd <pid> GC.class_histogram` → 存在 `net.minecraft.client.Minecraft`，
   **不存在** `ave`（运行时真的叫 MCP 名！）
4. **运行时成员名**：DLL 探测日志 `probe.log` → `mc.func_71410_x:Y`（SRG 成员名）

---

## 三、调试历程（完整编年史）

> 从初始开发到三个 Forge 版本全部真机验证的完整过程。
> 每个阶段记录：现象 → 假设 → 验证 → 结论，以及踩到的坑。

### 阶段 0：初始开发（假客户端全过）

**做法**：先做了 5 套假客户端（MCP/混淆/Forge 变体），用普通 java 启动，
注入验证判定逻辑。**全部通过，信心满满**——结果真机全翻车。

**教训（后来才懂）**：假客户端是单一类加载器环境，测不出任何加载器问题。
它不是没用——它验证了 JNI 调用逻辑和判定公式本身是对的，
把"代码写错"和"环境不同"两类问题隔离了。

### 阶段 1：Forge 1.8.9 真机翻车（双份类副本）

| 现象 | 排查 | 结论 |
|---|---|---|
| 真机 `map=vanilla189` 但 `inGame=0`，`getMinecraft()` 返回 null | 检查 JNI 找到的 `ave` 的静态字段 `S`（theMinecraft）为 null | app loader 从 -cp 加载了**另一份** ave 副本，从未被游戏 main 初始化 |
| `LaunchClassLoader` 加载 `ave` 抛 ClassNotFoundException | 反编译 jar：磁盘上 ave 存在 | **运行时类名已不是 ave**（被 FML 转成 MCP 名） |

**教训**：磁盘 ≠ 运行时；同一个类名在不同类加载器下是两个不同的类。

### 阶段 2：JVM 崩溃（两个经典 JNI 坑）

| 现象 | 根因 |
|---|---|
| 注入后 JVM 段错误 | `GetMethodID` 的第一个参数传了 `GetObjectClass(obj)` 的结果（实例的类），而不是方法**所属**的类 → 找到错误方法 ID（如 `ClassLoader.getName`）→ 调用时类型不匹配 → 崩溃 |
| 解析失败但错误详情丢失 | 异常记录代码同样用错类取 `getName` → 挂起异常未清理 → **pending exception 污染**后续所有 JNI 调用（静默返回 NULL） |

**教训**：`GetObjectClass(obj)` 的返回值不能用于 `GetMethodID`；
每个 JNI 调用失败路径必须 `ExceptionClear`。

### 阶段 3：类加载器定位（三工具定乾坤）

| 工具 | 用途 | 关键发现 |
|---|---|---|
| `javap -p` | 反编译磁盘 jar | 磁盘是原版混淆（ave/A/h/s） |
| `jmap -clstats <pid>` | 每个类加载器的类数 | LaunchClassLoader 3872 类（游戏类所在） |
| `jcmd GC.class_histogram` | 已加载类名 | 有 `net.minecraft.client.Minecraft`、无 `ave` |
| DLL 内 probe.log 探测 | 逐类/逐成员探测 | `func_71410_x:Y`（成员=SRG 名） |

**结论**：Forge 1.8.9 运行时 = **类名 MCP 名 + 成员 SRG 名**（`forge189mcp`）。

### 阶段 4：Forge 1.8.9 成功

1. 修正 `Launch.classLoader` 字段签名（声明类型是 `LaunchClassLoader` 不是 `ClassLoader`）
2. 新增 `forge189mcp` 映射
3. 禁用 JVMTI 修正（JDK 24 无 agent 时崩溃，且真机不需要）
4. 真机实测：`map=forge189mcp canAttack=1 target=EntityCreeper` ✅

**调试手法**：多 worker 竞争污染共享内存 → 改用文件日志（probe.log）；
老 worker 无法远程杀死 → 靠 failCount 超时 + 重启游戏。

### 阶段 4.5："假成功死循环"与终极修复（V54）

**现象**（后来复现）：1.8.9 Forge 再次出现 `map=vanilla189` + `mcNull=1` 死循环——
app loader 的**副本**（混淆名 ave）总能"解析成功"（成员名全对），
但它从未被游戏初始化（getMinecraft 返回 null）→ 翻转加载器 → 真加载器上
无混淆名类 → 失败 → 又翻回副本 → **永远轮不到正确的 forge189mcp**。

**根因**："解析成功"只证明"类结构匹配"，不证明"拿到了游戏实例"。

**终极修复（V54）**：解析成功后**立即调用 getMinecraft() 验证**——
返回 null 就是副本，判无效继续试下一个组合：
```
app loader + vanilla189 → 解析成功但 getMinecraft()=null → 判无效
LaunchClassLoader + forge189mcp → 解析成功 + 实例 ✓ → 采用
```

**意义**：这是双份副本问题的**最终判定法**——不再依赖加载器猜测，
直接以"能否拿到真实例"为准。所有环境（原版/Forge/假客户端）通用。

### 阶段 5：Forge 1.20.1（第二轮连环坑）

| 现象 | 根因 | 修复 |
|---|---|---|
| `forName(net.minecraft.client.Minecraft)` 在 app loader 上 CNFE | 游戏类在 `TransformingClassLoader`（ModLauncher），不在 app loader | 恢复线程遍历（主线程 context loader），加 10 秒超时 |
| 线程遍历找到 TCL 后 forName 仍 CNFE | ModLauncher **重写 loadClass**，不走标准流程 | **`findLoadedClass` 优先**（已加载的类直接拿） |
| findLoadedClass 时灵时不灵（DeepProbe OK / 解析循环 miss） | **pending exception 污染**（LoadClass 之间残留异常） | flc 调用前强制 `ExceptionClear` |
| useGameLoader 翻转导致用错加载器（指针对比 0x...e30 vs 0x...a170 揪出） | 翻转状态与解析循环不同步 | **双加载器同轮尝试**（TCL 和 app loader 都试） |
| 官方/混淆/SRG 成员名全不存在 | **成员名 = MCP stable 名**（`m_91097_` 格式，Forge reobf 目标） | 从本地 `mcp_config` 查 stable 映射，新增 `forge1201stb` |

**排查工具链**：`jmap -clstats` → `jcmd class_histogram` → DLL 反射枚举
方法名（`getDeclaredMethods`）→ 本地 `mcp_config` stable 映射。
真机实测：`map=forge1201stb canAttack=1 target=Creeper` ✅

### 阶段 5.5：V55 真机回归——加载器误选 + 功能改造

**背景**：V55 改造（5ms 检测周期 + UDP 35785 端口 0/1 上报 + 删除
调试日志）后，在 1.20.1 Forge 真机注入出现解析失败：
`errMsg=forName(LivingEntity) -> ClassNotFoundException`，且 loader 始终
是 `AppClassLoader`（永远解析不成功）。而同环境注入**原版 DLL 却成功**
（`loader=TransformingClassLoader map=forge1201stb`）。

**排查过程**（诊断 DLL 枚举线程 + 实测每个 context loader 的加载能力）：
1. `AppClassLoader` 加载 `net.minecraft.client.Minecraft` → CNFE（游戏 jar 在 module path）
2. `TransformingClassLoader` 加载 LivingEntity/Minecraft → 全部 OK（Render thread 等大量线程的 context loader）
3. 但 `FindThreadClassLoader` 却命中了 **`AppClassLoader`**——因为它能加载
   `ave`（1.8.9 混淆类名，用户 classpath 残留所致）！

**根因**：`FindThreadClassLoader` 的验证标准太弱——"任一 mcClass 可加载
就选中"。线程 Map（HashMap）遍历顺序**随机**：运气好先遇到
TransformingClassLoader 线程就成功（原版 DLL），运气差先遇到
AppClassLoader 线程就误选（V55 DLL），且永不重试 → 永久失败。

**修复（V55.1）**：验证标准改为**首选标准类名**：
```
loader 能加载 net/minecraft/client/Minecraft (MCP/Mojang 名, 1.8.9~1.20.1 通用)
  → 首选选中
否则只能加载 ave/enn 等混淆名 → 仅作后备 (found2)
```
真机实测：稳定命中 `TransformingClassLoader`，`map=forge1201stb` ✅

**同轮加固**（防半路失效）：
- 映射解析失败不再 2 分钟退出，无限重试
- 等待 jvm.dll 由 60 秒上限改为无限（仅进程退出才停止）
- `nullMcStreak` 阈值随 5ms 周期调整为 100（保持 0.5 秒容错）
- 修复 `kAllMaps[mapIdx - 1]` 数组越界（改为 `[mapIdx]`）

### 阶段 6：Forge 1.12.2（规律验证，一次通过）

**现象**：`vanilla189 :member` / `forge189 :member`——ave 类存在但成员名不对
（ave 是 1.12.2 的 BlockYellowFlower，不是 Minecraft）。

**排查**：反射枚举方法名 = `func_71386_F` 格式（SRG 名）→ 提取
`deobfuscation_data-1.12.2.lzma`（python lzma 解压）→ 查到：
- Minecraft = `bib`（运行时类名是 MCP 名，`mccls:OK` 实测确认）
- getMinecraft = `func_71410_x`（**与 1.8.9 相同！**）
- thePlayer/objectMouseOver/typeOfHit/entityHit/canAttackWithItem/isEntityAlive
  的 SRG 名全部与 1.8.9 相同
- 唯一差异：`MovingObjectPosition` → `net.minecraft.util.math.RayTraceResult`（1.12 改名）

**修复**：新增 `forge1122` 映射（= forge189mcp 只改 2 个类名）。
真机实测：`map=forge1122 canAttack=1 target=EntityCreeper` ✅ **一次通过！**

**意义**：验证了"SRG 名跨 1.8-1.12 稳定"规律——查表式扩展正式可行。

### 阶段 7：完整时间线（含版本迭代）

```
V1~V14   假客户端时代 + Forge 1.8.9 排查（双份副本 → JVM 崩溃 → 类加载器定位）
V15~V28  Forge 1.8.9 成功（Launch.classLoader 签名 + forge189mcp + JVMTI 禁用）
V29~V52  Forge 1.20.1 排查（TCL 获取 → findLoadedClass → pending 清理 →
         双加载器 → MCP stable 名）
V53      1.12.2 一次通过（forge1122）
V54      假成功死循环修复（解析后验证 getMinecraft 实例, 排除副本）
```

**迭代手法**：每次改动保留编译产物（dll_Vxx.dll）+ 新文件名注入
（绕 LoadLibrary 缓存）+ writer 清状态（绕 guard）+ 二分回归。
## 四、仍未完全解开的困惑（如实记录）

1. **JVMTI 崩溃机制**：JDK 24（假客户端）无 agent 时 `GetEnv(JVMTI)` +
   `GetLoadedClasses` 导致 JVM 崩溃；JRE 8（用户游戏）同代码未崩。
   推测是 JVM 版本对"运行时获取 JVMTI"的支持差异（无 agent 时能力受限），
   未深挖确认。**对策**：默认禁用 JVMTI 修正（`-DNO_REAL_FIX` 编译）。

2. **标准 Forge 1.8.9 是否同样结构**：用户环境（PCL + OptiFine preview +
   未修补 jar + `-Dfml.ignorePatchDiscrepancies=True`）实测为
   "MCP 类名 + SRG 成员"。官方安装器安装的纯 Forge 1.8.9 未实测，
   推测同为 FML DeobfuscationTransformer 机制（应一致），但未证实。

3. **线程遍历为何找不到游戏加载器**：理论上游戏主线程的 context
   classloader 被 launchwrapper 设为 LaunchClassLoader，遍历应命中。
   实测未找到（或卡死）。**对策**：改用 `Launch.classLoader` 直取，绕开此问题。

4. **env 探测矛盾**：`optifine` 标志类在 mods jar（不在系统 classpath），
   用 `FindClass` 却能检测到——类加载器可见性的具体机制未完全想通
   （不影响功能，仅诊断显示）。

5. **早期版本 loaderName 显示 LaunchClassLoader 的矛盾**：当时字段签名
   是错误的（按理论应失败），却显示拿到了 LaunchClassLoader——
   可能当时代码路径不同（线程遍历兜底）或 JNI 行为宽容，未回溯验证。

---

## 五、方法论总结

1. **反编译先行**：`javap` 看磁盘 jar 的类/成员名，建立基线
2. **运行时验证**：`jmap`/`jcmd` 看真实 JVM 内部（类加载器、已加载类）
3. **探测日志**：DLL 内逐类探测 + 文件日志（避免共享内存竞争干扰）
4. **二分定位**：保留每个历史版本，逐个回归找到崩溃引入点
5. **假客户端 ≠ 真机**：假客户端（单一类加载器）无法暴露多加载器问题，
   但适合快速验证逻辑；真机验证不可替代

---

## 六、Forge 运行时命名规律（实测总结）

> 基于本项目两个真机环境（Forge 1.8.9 + OptiFine、Forge 1.20.1）的实测数据，
> 结合 Forge 版本史整理。**带 ✅ 的是本项目实测验证过的。**

### 6.1 规律总表：Forge 三代体系的运行时结构

| Forge 时代 | 代表版本 | 类名（运行时） | 成员名（运行时） | 游戏加载器 | 入口 | 验证 |
|---|---|---|---|---|---|---|
| FML 1（安装期 patch） | 1.6.4 ~ 1.7.10 | 混淆名 | SRG 名 | LaunchClassLoader | launchwrapper | 未实测（文档推断） |
| FML 2（运行时反混淆） | **1.8 ~ 1.12.2** | **MCP 名** | **SRG 名** | LaunchClassLoader | launchwrapper + FMLTweaker | ✅ 1.8.9 / **1.12.2** 实测 |
| 过渡期 | 1.13 ~ 1.16.5 | 混合/待确认 | 混合/待确认 | ？ | ？ | 未实测 |
| ModLauncher | **1.17+** | **Mojang 官方名** | **MCP stable 名** | TransformingClassLoader | ModLauncher | ✅ 1.20.1 实测 |

### 6.2 规律 1：类名和成员名是**两套独立体系**，分开演变

- **类名**：磁盘上永远是混淆名（`ave`/`enn`）；运行时被转换成
  开发映射的类名（1.8-1.12 = MCP 名，1.17+ = Mojang 官方名）
- **成员名**：运行时 = **reobf 目标**（mod 发布时从开发名重映射到的名字）
  - 1.8-1.12：SRG 名（`func_71410_x` / `field_71439_g`）✅
  - 1.17+：MCP stable 名（`m_91087_` / `f_91074_`）✅

### 6.3 规律 2：Forge 运行时类名 = 开发环境的类名

Forge 把游戏类反混淆/重映射成**mod 开发时引用的类名**，
所以 mod 不需要 reobf 类名（只 reobf 成员名）：

- 1.8-1.12 开发用 MCP 名 → 运行时类名 = MCP 名（`net.minecraft.client.Minecraft`）✅
- 1.17+ 开发用 Mojang 名 → 运行时类名 = Mojang 名（`net.minecraft.client.Minecraft`）✅

**推论**：想快速判断一个 Forge 版本的运行时类名，
查它的开发映射用什么类名就行，不用猜。

### 6.4 规律 3：SRG 名在 1.8-1.12 跨版本**大体**稳定（✅ 已实锤，含例外）

`func_71410_x`（getMinecraft）、`field_71439_g`（thePlayer）、`field_71476_x`（objectMouseOver）、
`field_72313_a`（typeOfHit）、`field_72308_g`（entityHit）、`func_70075_an`（canAttackWithItem）、
`func_70089_S`（isEntityAlive）在 **1.8.9 和 1.12.2 完全相同**（两个版本真机验证）。

**类名例外（已验证）**：1.12 把 `net.minecraft.util.MovingObjectPosition` 改名为
`net.minecraft.util.math.RayTraceResult`——所以 1.12.2 的映射只比 1.8.9 多改这一行
（`forge1122` = `forge189mcp` 只改 mopClass/typeClass 两个类名）。

**成员名例外（1.9+ 双持系统，放置物检测真机暴露）**：**无参 `getHeldItem`
（SRG `func_70694_bm`）在 1.9 起被移除**，改为带参 `getHeldItem(EnumHand)`
（`func_184586_b`）和无参主手取物 `getHeldItemMainhand`（`func_184614_ca`）。
→ **1.8.9 用 `func_70694_bm`，1.12.2 必须用 `func_184614_ca`**（详见第七章 7.5）。

**推论（已实锤）**：1.9/1.10/1.11 的 Forge 映射 = 抄 `forge1122` 微调即可；
查名工具：Forge 安装的 `deobfuscation_data-<版本>.lzma`（python lzma 可解，
格式 `FD:/MD: 混淆名 SRG名`）。注意**新增检测项时必须逐方法核对**
SRG 名在目标版本是否存在——"跨版本稳定"只适用于已被真机验证过的那几个成员。

### 6.5 规律 4：MCP stable 名随版本变化，但同一版本全家桶一致

- `m_91087_` 是 1.20.1 的 getInstance；1.19、1.20.4 的数字不同
- 同一版本内 Forge / NeoForge 的 stable 名**完全一致**

**推论**：支持一个新 1.17+ 版本 = 从该版本的 mcp_config 查一套 stable 名。
本地就有：`libraries/de/oceanlabs/mcp/mcp_config/<版本>/mappings-merged.txt`
（tsrg2 格式：`混淆名 签名 stable名`）

### 6.6 规律 5：加载器体系分两大代

| 时代 | 加载器 | 获取方式 | 特性 |
|---|---|---|---|
| 1.8-1.12 | `net.minecraft.launchwrapper.LaunchClassLoader` | `Launch.classLoader` 静态字段（**字段类型是 LaunchClassLoader，不是 ClassLoader**）✅ | `findLoadedClass`/`forName` 都可用 |
| 1.17+ | `cpw.mods.modlauncher.TransformingClassLoader` | 线程遍历（主线程 context classloader）✅ | **`forName` 不可用（重写 loadClass），`findLoadedClass` 可用** |

**推论**：通用适配策略 = 加载器候选列表依次尝试 + 每个加载器内
`findLoadedClass` 优先、`forName` 兜底（本项目 V49 双加载器同轮尝试）。

### 6.7 规律 6：快速判定一个 Forge 版本运行时结构的四步法

```
1. javap 磁盘 jar        → 混淆名基线（ave/A()/enn/N()）
2. jmap -clstats <pid>   → 游戏加载器（LaunchClassLoader / TransformingClassLoader）
3. jcmd class_histogram  → 运行时类名（MCP? Mojang? 混淆?）
4. DLL 反射枚举方法名    → 成员名格式（A? func_? m_? getInstance?）→ 查映射定名
```

本项目两个版本都靠这套四步法定案，一次到位。

---

## 七、放置物检测（canPlace）扩展记录

> 在 canAttack 之上新增的第二个检测项：**手持物品是否为放置物**
> （1.8.9/1.12.2 = ItemBlock，1.20.1 = BlockItem）。判据只用
> `getHeldItem/getMainHandItem → getItem → instanceof ItemBlock/BlockItem`
> 一条链，与准星无关。

### 7.1 各体系映射（全部查自本地文件，非猜测）

| 映射 | getHeldItem (玩家) | ItemStack 类 | getItem | Item 类 | ItemBlock 类 |
|---|---|---|---|---|---|
| mcp189 | `getHeldItem` `()Lnet/minecraft/item/ItemStack;` | `net/minecraft/item/ItemStack` | `getItem` `()Lnet/minecraft/item/Item;` | `net/minecraft/item/Item` | `net/minecraft/item/ItemBlock` |
| vanilla189 | `bA` `()Lzx;` | `zx` | `b` `()Lzw;` | `zw` | `yo` |
| forge189 | `func_70694_bm` `()Lzx;` | `zx` | `func_77973_b` `()Lzw;` | `zw` | `yo` |
| forge189mcp | `func_70694_bm` `()Lnet/minecraft/item/ItemStack;` | `net/minecraft/item/ItemStack` | `func_77973_b` `()Lnet/minecraft/item/Item;` | `net/minecraft/item/Item` | `net/minecraft/item/ItemBlock` |
| forge1122 | **`func_184614_ca`**（getHeldItemMainhand）`()Lnet/minecraft/item/ItemStack;` | `net/minecraft/item/ItemStack` | `func_77973_b` `()Lnet/minecraft/item/Item;` | `net/minecraft/item/Item` | `net/minecraft/item/ItemBlock` |
| vanilla1122 | `co` `()Laip;` | `aip` | `c` `()Lain;` | `ain` | `ahb` |
| vanilla1201 | `eO` `()Lcfz;` | `cfz` | `d` `()Lcfu;` | `cfu` | `cds` |
| forge1201obf | `eO` `()Lnet/minecraft/world/item/ItemStack;` | `net/minecraft/world/item/ItemStack` | `d` `()Lnet/minecraft/world/item/Item;` | `net/minecraft/world/item/Item` | `net/minecraft/world/item/BlockItem` |
| forge1201stb | `m_21205_` `()Lnet/minecraft/world/item/ItemStack;` | `net/minecraft/world/item/ItemStack` | `m_41720_` `()Lnet/minecraft/world/item/Item;` | `net/minecraft/world/item/Item` | `net/minecraft/world/item/BlockItem` |
| forge1201 | `getMainHandItem` `()Lnet/minecraft/world/item/ItemStack;` | `net/minecraft/world/item/ItemStack` | `getItem` `()Lnet/minecraft/world/item/Item;` | `net/minecraft/world/item/Item` | `net/minecraft/world/item/BlockItem` |

### 7.2 映射来源（本地文件实锤）

1. **1.8.9**：`Desktop\1.8.9\joined.srg`
   - `MD: pr/bA ()Lzx; .../func_70694_bm ()Lnet/minecraft/item/ItemStack;`
   - `MD: zx/b ()Lzw; .../func_77973_b ()Lnet/minecraft/item/Item;`
   - `CL: yo net/minecraft/item/ItemBlock`、`CL: zw net/minecraft/item/Item`
   - `getHeldItem` 定义在 EntityLivingBase（`pr`）上，玩家（`wn`）继承——方法 ID 用 `liv`（livingClass）解析
2. **1.20.1 混淆名**：`verify\client-1.20.1-mappings.txt`（Mojang 官方映射）
   - `LivingEntity getMainHandItem() -> eO`（行 59544，返回 `Lcfz;`）
   - `ItemStack getItem() -> d`（cfz 段，返回 `Lcfu;`）
   - `ItemBlock -> cds`（1.20.1 改名 BlockItem）
3. **1.20.1 MCP stable 名**：`libraries\...\mcp_config\1.20.1-20230612.114412-mappings-merged.txt`（tsrg2）
   - `eO ()Lcfz; m_21205_`、`d ()Lcfu; m_41720_`（行 36522 / 67674）
4. **1.12.2 SRG 名**：Forge 1.12.2 jar 内 `deobfuscation_data-1.12.2.lzma`（python lzma 解压）
   - `MD: vp/co ()Laip; .../func_184614_ca ()Lnet/minecraft/item/ItemStack;`（getHeldItemMainhand）
   - `MD: aip/c ()Lain; .../func_77973_b ()Lnet/minecraft/item/Item;`（ItemStack.getItem）
   - `CL: ahb net/minecraft/item/ItemBlock`
5. **1.12.2 混淆名**（`vanilla1122`，同一 lzma 文件）：
   - `CL: bib net/minecraft/client/Minecraft`、`MD: bib/z ()Lbib;`（getMinecraft）
   - `FD: bib/h net/minecraft/client/Minecraft/field_71439_g`（thePlayer）、`FD: bib/s .../field_71476_x`（objectMouseOver）
   - `CL: vg net/minecraft/entity/Entity`、`CL: vp net/minecraft/entity/EntityLivingBase`、`CL: bud net/minecraft/client/entity/EntityPlayerSP`
   - `CL: bhc net/minecraft/util/math/RayTraceResult`、`FD: bhc/a .../field_72313_a`、`FD: bhc/d .../field_72308_g`、`FD: bhc$a/c .../ENTITY`
   - `CL: aip net/minecraft/item/ItemStack`、`CL: ain net/minecraft/item/Item`、`CL: ahb net/minecraft/item/ItemBlock`

### 7.3 设计要点

- **可选解析**：放置物成员在 `ResolveWith` 中是可选解析——任一环节失败
  只清异常 + 留 NULL，`canPlace` 恒 0，**绝不 return false 拖垮 canAttack**。
  这是与 canAttack 成员（全有才成功）的关键区别，保证真机任何环境
  canAttack 检测不回退。
- **判定与准星无关**：canPlace 在 `UpdateStatus` 里于 mop 检查**之前**
  独立计算（用 player 取手持），未瞄准时仍正常上报（UDP byte1）。
  早期实现放在 `!player || !mop` 提前返回之后导致未瞄准时 canPlace 恒 0，
  假客户端阶段 5 暴露后修正。
- **UDP 协议**：35785 端口包改为 **2 字节** `[byte0=canAttack][byte1=canPlace]`。
  byte0 与旧版 1 字节协议完全一致 → 旧接收端零改动兼容。
- **共享内存**：`CanAttackStatus` 新增 `canPlace`、`placeReady`（放置物链
  是否解析成功，诊断用——**区分"真空手"与"链未解析成功"**）、
  `heldItemNull`、`heldItemName[64]`（手持 Item 类名诊断），`version` 4 → 6。

### 7.4 测试结果

**假客户端 7 套全部通过**（UDP 逐秒聚合 / 共享内存采样验证，`11`/`10`/`01`
组合符合预期；vanilla1122 假客户端曾因测试代码用 `new vg()`（Entity 基类）
当瞄准目标导致 canAttack 恒 0——`IsInstanceOf(livingClass)` 判定不过，
修正为目标用 `new vp()`（EntityLivingBase）后通过）：
- 阶段1 未瞄准+空手 → `00`
- 阶段2 瞄准+手持 ItemBlock → `11`
- 阶段3 瞄准+手持普通 Item → `10`
- 阶段4 瞄准+空手 → `10`
- 阶段5 未瞄准+手持 ItemBlock → `01`

**真机三环境全部通过（实测）**：

| 环境 | map= | 手持剑 | 手持放置物 | canAttack |
|---|---|---|---|---|
| Forge 1.8.9（PCL+OptiFine） | `forge189mcp` | canPlace=0（ItemSword）✅ | canPlace=1（ItemColored）✅ | ✅ |
| Forge 1.12.2 | `forge1122` | canPlace=0（切剑后）✅ | canPlace=1（ItemColored）✅ | ✅ |
| Forge/NeoForge 1.20.1 | `forge1201stb` | canPlace=0（SwordItem）✅ | canPlace=1（BlockItem）✅ | ✅ |

**原版真机（补测）**：

| 环境 | map= | 手持剑 | 手持放置物 |
|---|---|---|---|
| 原版启动器 1.8.9 | `vanilla189` | canPlace=0（aay=ItemSword）✅ | canPlace=1（方块）✅ |
| 原版启动器 1.12.2 | `vanilla1122` | canPlace=0（ajy=ItemSword）✅ | canPlace=1（ajz=ItemColored）✅ |
| 原版启动器 1.20.1 | `vanilla1201` | canPlace=0（chk=SwordItem）✅ | canPlace=1（cds=BlockItem）✅ |

原版无 launchwrapper/ModLauncher，直接走 AppClassLoader + 混淆名，加载器
路径最简单，一次命中。至此**三个原版版本全部真机实测通过**。

### 7.5 真机踩坑：1.12.2 双持系统（`func_70694_bm` → `func_184614_ca`）

**现象**：1.12.2 真机注入后 `map=forge1122` 解析成功、canAttack 正常，
但手持方块时 `canPlace` 恒 0。`heldItemNull=1` 无法区分"真空手"还是
"链没解析成功"——新增 `placeReady` 诊断字段后确认是**后者**。

**根因**：**1.9 起 Minecraft 引入双持系统，无参 `getHeldItem()` 被移除**。
1.8.9 的 `func_70694_bm`（无参 getHeldItem）在 1.12.2 的
`deobfuscation_data-1.12.2.lzma` 中**查无此条**，取而代之：
- `func_184614_ca ()LItemStack;` = **getHeldItemMainhand()**（无参，取主手）
- `func_184586_b (LEnumHand;)LItemStack;` = getHeldItem(EnumHand)（带参数）

**修复**：`forge1122` 映射的 `heldItemGetter` 从 `func_70694_bm` 改为
`func_184614_ca`。真机复测：手持方块 `canPlace=1` ✅、切剑后 `canPlace=0` ✅。

**教训**：技术文档原先的"SRG 名在 1.8-1.12 跨版本稳定"只对 **canAttack
相关成员**成立（func_71410_x 等真机验证过）；**新增检测项时必须逐方法
用 `deobfuscation_data-<版本>.lzma` 核对 SRG 名在目标版本是否存在**。
好在"可选解析"设计让这次错误只影响 canPlace（静默降级），**没有破坏
canAttack**——这正是 7.3 可选解析设计的价值。
